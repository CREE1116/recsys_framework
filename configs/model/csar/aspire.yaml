model:
  name: "aspire"
  alpha: 500.0 # Same as regularize, but explicit name
  beta: 0.5 # Tikhonov exponent range (0 ~ 2)
  target_energy: 0.99
  visualize: false # Skip heavy visualizations for Bayesian searches
# Adaptive Spectral Popularity-aware Intrinsic REgularization
