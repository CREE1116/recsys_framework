model:
  name: "aspire"
  alpha: 500.0

  # Beta: MCAR 기준선(a=0.5)으로부터의 스펙트럼 이탈량
  # 'auto': β = max(0, 2a - 1), SWLS로 a 추정
  beta: "auto"
  estimator_type: "lad"
  target_energy: 0.95
  visualize: true
# Adaptive Spectral Popularity-aware Intrinsic REgularization
